/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export",
    images:{
        domains: ['images.pexels.com', 'images.unsplash.com', 'source.unsplash.com','avatars.githubusercontent.com']
    
    }
};

export default nextConfig;
